import React from 'react';

export const FunctionAndClass = (props) =>
{
  return (
    <div>
      <h1>{props.name} works as {props.work}</h1>
   
    </div>
  )
}
export default FunctionAndClass;